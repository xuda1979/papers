% Lightweight, safe-to-include preamble improvements.
% You can `\input{tex/preamble_recommendations}` at the end of your preamble,
% or copy/paste selectively. Designed to avoid conflicts in most templates.
% ---- Typography & links
\usepackage{microtype}          % better kerning & hyphenation
\usepackage[hidelinks]{hyperref}% robust hyperlinks without boxes
\usepackage[nameinlink,capitalise,noabbrev]{cleveref} % smart cross-refs
\usepackage{booktabs}           % professional tables
\usepackage{siunitx}            % units & aligned numbers
\sisetup{detect-all,group-minimum-digits=4}
\usepackage{csquotes}           % proper quotes in text

% ---- Nice compact lists without global style changes
\usepackage{enumitem}
\setlist{nosep,leftmargin=*,labelsep=0.5em}

% ---- Algorithm float (uncomment one family as needed)
% \usepackage[algo2e,ruled,vlined]{algorithm2e}
% \usepackage{algorithm}
% \usepackage{algpseudocode}

% ---- Figure & table helpers
\usepackage{graphicx}
\usepackage{subcaption} % subfigures with `\begin{subfigure}`

% ---- Handy macros (rename if your template already defines these)
\newcommand{\ttc}{test-time compute}
\newcommand{\TTC}{Test-Time Compute}
\newcommand{\ours}{\textsc{DeliberativeCompute}}
\newcommand{\etal}{et~al.\ }
\newcommand{\eg}{e.g.,\ }
\newcommand{\ie}{i.e.,\ }

% ---- Reference examples:
% \cref{fig:scaling}  --> "Figure 2"
% \Cref{thm:main}     --> "Theorem 1"
% \cref{alg:knockout} --> "Algorithm 1"

% ---- Table example (paste into your paper and edit)
% \begin{table}[t]
%   \centering
%   \caption{Quality vs. TTC on GSM8K. Means ± 95\% CI over 5 seeds.}
%   \label{tab:ttc_scaling}
%   \begin{tabular}{lrrrr}
%     \toprule
%     Method & N & Tokens & Calls & Acc. (\%) \\
%     \midrule
%     Greedy CoT         & 1  &  800 & 1  & 57.3 \\
%     Self-consistency   & 16 & 6400 & 16 & 72.1 \\
%     \ours (verify+rev) & 8  & 5200 & 12 & \textbf{74.0} \\
%     \bottomrule
%   \end{tabular}
% \end{table}

% ---- Safety: avoid double-loading if your template already includes a package.
% If conflicts arise, comment out the offending lines above.
