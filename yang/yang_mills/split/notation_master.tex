% notation_master.tex
% Defines the "Contracts" between chapters.
% Global Constants and Stub Theorems.

\usepackage{amsmath, amsthm, amssymb}

% Global Constants
\newcommand{\BaseGap}{\gamma_0}
\newcommand{\Coupling}{\beta}
\newcommand{\LatticeSize}{L}
\newcommand{\UniformGap}{\gamma}

% Create theorem environments if they don't exist
\ifdefined\newtheorem
\else
  \usepackage{amsthm}
\fi

\ifdefined\theoremI
\else
  \newtheorem{theoremI}{Theorem}[chapter]
\fi

\ifdefined\theoremII
\else
  \newtheorem{theoremII}{Theorem}[chapter]
\fi

\ifdefined\theoremIII
\else
  \newtheorem{theoremIII}{Theorem}[chapter]
\fi

\ifdefined\theoremIV
\else
  \newtheorem{theoremIV}{Theorem}[chapter]
\fi

% Stub Theorems (Interface Contracts)
\newcommand{\StatementTheoremI}{There exists a computable constant \BaseGap > 0 such that for a fixed lattice of size \LatticeSize at coupling \Coupling, the spectral gap is \BaseGap.}

% Chapter II Contract
\newcommand{\StatementTheoremII}{Assuming Theorem I.1 holds (and treating \BaseGap as a variable), if the base block has gap \BaseGap, then the infinite lattice has a uniform gap \UniformGap.}

% Chapter III Contract
\newcommand{\StatementTheoremIII}{Assuming Theorem II.1 holds (uniform bounds exist), the limit a \to 0 defines a valid Hilbert space with a positive Hamiltonian.}

% Chapter IV Contract
\newcommand{\StatementTheoremIV}{The interpolation from Strong to Weak coupling does not cross a phase transition. (Lattice Witten Index argument).}

% Conditional phrasing helper
\newcommand{\ConditionalProof}[2]{\noindent\textbf{Proof (Conditional on #1):} #2 \qed}
