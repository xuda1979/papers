# Alternative roadmaps

Alternative routes still point back to the same theorem boundary and to `split/app151_hamiltonian_identification_theorem_boundary.tex`.

- dictionary lemma
- single-letter representatives
- common-realization witness package on `w_n` and `f_n`
- injective letter-level dictionary
- letter-level injectivity of the packet dictionary
- generator-level injectivity
- reflection-extension injectivity
- labeled representative choice
- a witness package on `w_n` and `f_n` must remain shared across both branches
- unresolved shortcut subgoals stay recorded in `blocked_actions`
- finite wilson-loop moment packet agreement is still downstream of the common-realization package
