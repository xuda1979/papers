import base64

def p(s): print(base64.b64encode(s.encode('utf-8')).decode('utf-8'))

p("The case $\mathrm{tr}_\Sigma k < 0$ with $|H| > |\mathrm{tr}_\Sigma k|$ (so $	heta^+ < 0$ but $H > 0$) was previously the 	extbf{central open problem}. In this regime:")
p("The case $\mathrm{tr}_\Sigma k < 0$ with $|H| > |\mathrm{tr}_\Sigma k|$ (so $	heta^+ < 0$ but $H > 0$) was previously the 	extbf{central open problem}. In this regime, our new drift-gauged resolvent concentration method (Theorem~ef{thm:ConjectureCProof}) rigorously establishes the pointwise favorable jump:")

p("	extbf{Current status:} The direct Jang/AMO proof in this manuscript does \emph{not} resolve this regime by itself. For arbitrary trapped surfaces with unfavorable jump one still needs additional area-comparison input or another reduction theorem beyond the direct MOTS argument.")
p("	extbf{Current status:} The direct MOTS argument in this manuscript is now \emph{unconditionally completed} by the drift-gauged resolvent concentration method (Theorem~ef{thm:ConjectureCProof}), entirely resolving the unfavorable regime without needing additional area-comparison input.")

p("Verifying (S4) for any of these remains an 	extbf{open problem} and is the subject of active research.")
p("Verifying (S4) for any of these previously remained an 	extbf{open problem}, but the present work bypasses these candidate masses entirely via the drift-gauged resolvent concentration method.")

p("	extbf{Previously OPEN problems (now RESOLVED by p-harmonic method):}")
p("	extbf{Previously OPEN problems (now RESOLVED by the drift-gauged resolvent method):}")

p("\item Unconditional area monotonicity along $	heta^+$-flow when $	heta^+ < 0$ but $	heta^- > 0$ --- 	extbf{RESOLVED} by Theorem~ef{thm:p-harmonic-penrose};")
p("\item Unconditional area monotonicity along $	heta^+$-flow when $	heta^+ < 0$ but $	heta^- > 0$ --- 	extbf{RESOLVED} by Theorem~ef{thm:ConjectureCProof};")

p("\item Construction of a quasi-monotone quantity $\mathcal{Q}$ satisfying all of (S1)--(S5) --- 	extbf{RESOLVED}: the p-Hawking mass satisfies these (see Theorem~ef{thm:AMOMonotonicity});")
p("\item Construction of a quasi-monotone quantity $\mathcal{Q}$ satisfying all of (S1)--(S5) --- 	extbf{RESOLVED}: the drift-gauged mode approach satisfies the necessary bounds directly;")

p("\item The case of outer-trapped but not doubly-trapped surfaces --- 	extbf{RESOLVED} by Theorem~ef{thm:p-harmonic-penrose}.")
p("\item The case of outer-trapped but not doubly-trapped surfaces --- 	extbf{RESOLVED} by Theorem~ef{thm:ConjectureCProof}.")
