# Quantum coding LLM R&D — bounded improvement (2026-03-27)

## What I did
- Read AGENTS.md, USER.md, SOUL.md, HEARTBEAT.md, `skills/huanxin-browser/SKILL.md`, `skills/s3-transfer/SKILL.md`.
- Checked for PROJECT.md in this workspace and confirmed it is missing here.
- Read the most recent available memory note before acting (`memory/2026-03-22.md`).
- Inspected the current experiment/analysis scripts and found a recurring failure mode: several CSV writers assume `rows[0]` exists, which crashes with an opaque `IndexError` when an experiment or analysis produces no rows.
- Patched the affected scripts to fail fast with explicit `ValueError` messages instead:
  - `experiments/ai2_application_bench.py`
  - `experiments/ai2_torch_npu_microbench.py`
  - `experiments/analyze_ai2_application_bench.py`
  - `experiments/analyze_ai2_repeatability.py`
  - `experiments/benchmark_ephemeral_weights.py`
  - `experiments/calibrate_910b_model.py`
  - `experiments/plot_ai2_frontier.py`
  - `experiments/ai2_microbench.py`
- Ran local validation after the edits:
  - `python3 -m py_compile experiments/*.py`
  - `python3 experiments/benchmark_ephemeral_weights.py`
  - targeted regression check proving the new empty-row guard raises a clear `ValueError` in `experiments/ai2_microbench.py`

## What I learned
- This workspace does not currently contain `PROJECT.md`, so the heartbeat instruction to read it cannot be completed literally here.
- The experiment stack already has multiple independent writer helpers with the same fragile `rows[0]` pattern.
- Replacing the implicit `IndexError` with explicit `ValueError` turns a vague post-hoc crash into a direct explanation of the real problem (empty benchmark/summary/calibration input), which is a useful unblocker before any remote ai2 run.

## Next step
- Add one lightweight local smoke test that exercises one analysis path with intentionally empty or mismatched inputs, so future refactors keep these friendlier failure modes intact.
